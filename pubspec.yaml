name: clinic_admin
description: Admin dashboard for Clinic Card – manage doctors on Supabase.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  google_fonts: ^6.2.1
  fl_chart: ^0.69.0
  url_launcher: ^6.3.2
  http: ^1.6.0
  flutter_localizations:
    sdk: flutter

flutter:
  uses-material-design: true
